build:
  docker:
    web: Dockerfile
run:
  web: yarn ganache --db=/app/ganache_data --networkId '5777' --host '0.0.0.0' --port '7545' --mnemonic grape solution air laundry garage body erupt thought ritual renew ginger tackle
